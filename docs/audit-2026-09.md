# Reconciliation audit — 2026-09-03

**Read this when** you want to know which brief items are actually built, which
diverged, and where the fixes landed.

Scope: the original brief's steps 1-5 walked against the code as it stood at
commit `6c5050a`, before the September corrections. **A final section at the
bottom** walks the whole brief, all ten steps, as it stands after step 10 — go
there for a verdict on one item; the sections in between are the working notes
of each correction. Verdicts are
**implemented** / **partial** / **missing** / **diverged**. A diverged item has a
`DECISIONS.md` entry; if it does not, that is a finding, not a divergence.

Every "verified" line below was run, not reasoned about. Commands are quoted so
the next person can re-run them.

---

## 0. Process gates

| Item                                         | Verdict     | Notes                                                                                                                                                                                                      |
| -------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck` exits 0 across all packages | implemented | **Pre-fix error count: 0.** All five packages were clean on the installed Prisma 6.19.3. The reported "datasource property url is no longer supported" failure is a Prisma **7** error — see Correction 1. |
| typecheck is a hard CI gate                  | implemented | `.github/workflows/ci.yml` runs typecheck, lint, `format:check`, test and build as separate steps.                                                                                                         |
| no `@ts-expect-error` / `any` escape hatches | implemented | `grep -rn "ts-expect-error" apps packages scripts` returns nothing outside the generated Prisma client.                                                                                                    |

## 1. Monorepo scaffold

| Item                                              | Verdict     | Where                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| pnpm workspace, apps/\* + packages/\*             | implemented | `pnpm-workspace.yaml`                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Postgres + PostGIS, Redis, MinIO, MailHog         | implemented | `docker-compose.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `/health` with per-dependency detail and 503      | implemented | `apps/api/src/routes/health.ts`, D10                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ESLint 9 + Prettier + husky + lint-staged         | implemented | `eslint.config.mjs`, `.husky/pre-commit`                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CI: validate, generate, deploy, build, lint, test | implemented | `.github/workflows/ci.yml`                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **`api` service healthcheck**                     | implemented | Not a gap, and the brief's premise was wrong: there is no `healthcheck:` block in `docker-compose.yml`, but `apps/api/Dockerfile` ends with a `HEALTHCHECK` that fetches `/health`, and compose's `service_healthy` reads the container's health status whatever declared it. Verified: `docker inspect --format '{{json .Config.Healthcheck}}' machiya-api:verify` returns the test. A comment now points at the Dockerfile so the next reader does not repeat this finding. |
| **`worker` service healthcheck**                  | implemented | Same: `apps/worker/Dockerfile` carries a `HEALTHCHECK` against `/health` on `WORKER_PORT`. Not a gap.                                                                                                                                                                                                                                                                                                                                                                         |
| **`worker` SMTP env**                             | **missing** | Real gap. The worker sends the first-message enquiry notification in step 9; compose passed it no `SMTP_*` and its env schema declared none. Fixed.                                                                                                                                                                                                                                                                                                                           |
| `worker` S3 env for the sharp pipeline            | implemented | Already present — endpoint, region, bucket, both keys, `S3_FORCE_PATH_STYLE`, `KEEP_ORIGINALS`, `IMAGE_CONCURRENCY`.                                                                                                                                                                                                                                                                                                                                                          |
| maplibre worker URL fix                           | implemented | D12, `apps/web/scripts/sync-maplibre-worker.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                              |

## 2. Data model

| Item                                              | Verdict     | Where                                                                                                                                                                                        |
| ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| All models from the brief                         | implemented | `packages/db/prisma/schema.prisma`                                                                                                                                                           |
| lat/lng kept in sync with `location` by a trigger | implemented | `..._location_sync_trigger/migration.sql`, D2                                                                                                                                                |
| GiST indexes with the brief's names               | diverged    | Declared in the schema rather than hand-written, because Prisma introspects them and would otherwise emit a DROP on every `migrate dev`. D13.                                                |
| `location` NOT NULL                               | diverged    | A CHECK constraint, because a REQUIRED `Unsupported()` field removes the model's `create` operation. D15.                                                                                    |
| **`pg_trgm` declared and created**                | implemented | `datasource db { extensions = [postgis, pg_trgm] }`, plus `@@index([title(ops: raw("gin_trgm_ops"))], type: Gin)` and the same on `address`. Verified with `\dx` against the live container. |
| Keyset pagination, not OFFSET                     | implemented | D19                                                                                                                                                                                          |
| Bounded KNN for similar listings                  | implemented | D18                                                                                                                                                                                          |
| Money as Int rupees, fuel as Decimal              | implemented | D17                                                                                                                                                                                          |
| **`Locality` table**                              | **missing** | Localities existed only as literals in `scripts/cities.ts`. Correction 2's tier-1 autocomplete needs them queryable. Added.                                                                  |

## 3. Better Auth

| Item                                                 | Verdict     | Where                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Self-hosted Better Auth on the Prisma adapter        | implemented | `apps/api/src/auth/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Google + GitHub social providers, env-gated**      | implemented | Not a gap. `socialProviders()` wires a provider only when it is BOTH listed in `AUTH_ENABLED_PROVIDERS` and has a non-empty client id and secret; `SocialButtons.tsx` renders from `VITE_AUTH_PROVIDERS`.                                                                                                                                                                                                                                                                                                                                                                                   |
| **admin plugin mounted**                             | **wrong**   | Superseded by step 9. It was mounted and **inert**: `adminRoles` gets a caller past the is-an-admin gate, but every endpoint then resolves `roles[session.role]` against the plugin's built-in `admin`/`user` map, so `ADMIN` missed and all three of list-users, ban-user and set-role answered 403. This row is the audit's own worst miss — the method was "grep the config, confirm the option is present", the option was present, and the behaviour was absent. See [D70](../DECISIONS.md#d70-the-admin-plugin-was-mounted-and-inert-because-adminroles-is-not-the-permission-check). |
| **openAPI dev-only**                                 | implemented | Not a gap. `...(isProduction ? [] : [openAPI()])`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Database-backed sessions with cookie caching         | implemented | D20                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Two Redis connections with opposite failure policies | implemented | D21                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Schema checked against the runtime, not the CLI      | implemented | `pnpm auth:check`, D22                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **`requireRole` admin-override test**                | implemented | Not a gap. `apps/api/test/auth-guards.test.ts` — "admits an admin to a route that does not name ADMIN", plus "still rejects a lister from an admin-only route".                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **wrong-owner test**                                 | implemented | Not a gap. Same file — "rejects a different signed-in user with 403" asserting `error.code === 'forbidden_owner'`, and "lets an admin override ownership".                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## 4. Bootstrap, geo services and seed

| Item                                          | Verdict      | Notes                                                                                                                                                                                                                                                                                  |
| --------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Geofabrik download, osmium bbox cut, merge    | implemented  | `scripts/bootstrap.sh`, D27, D28                                                                                                                                                                                                                                                       |
| Photon                                        | **reversed** | Was deferred behind `--profile photon`. Correction 2 removes it outright and rewrites D26.                                                                                                                                                                                             |
| **Nominatim cluster path**                    | implemented  | Verified rather than assumed: `docker run --rm --entrypoint sh mediagis/nominatim:5.3 -c 'ls -d /var/lib/postgresql/*/main'` prints `/var/lib/postgresql/16/main`, and `/usr/lib/postgresql/` holds only `16`. The existing mount was already correct, so the import is not repeating. |
| **Nominatim flatnode volume**                 | **wrong**    | `nominatim-flatnode` was mounted at `/nominatim/flatnode` while `FLATNODE_FILE` was never set — an inert volume implying a tuning knob that was not in use. Deleted; three city extracts do not need a flatnode file.                                                                  |
| **OSRM image platform**                       | **missing**  | Verified: `docker manifest inspect --verbose osrm/osrm-backend:v5.25.0` returns a single manifest, not a manifest list, with `"platform": {"architecture":"amd64","os":"linux"}`. `platform: linux/amd64` added to all three OSRM services and stated in prerequisites.                |
| **`OSRM_CAR_PORT` default**                   | **diverged** | Was 5000, which is AirPlay Receiver on macOS. Now 5100.                                                                                                                                                                                                                                |
| **`osrm-init` guard**                         | **missing**  | With no `/data/merged.osm.pbf` the container died inside `osrm-extract` with an opaque libosmium error. Now exits early naming `pnpm bootstrap`.                                                                                                                                       |
| **`osrm-bike` healthcheck URL**               | **wrong**    | Used `/route/v1/bike/...`. `osrm-routed` ignores the profile segment entirely — the graph decides the profile — so the URL implied a distinction that does not exist. Both healthchecks now use `driving`.                                                                             |
| **`packages/shared/geo` provider interfaces** | **missing**  | Only `packages/shared/src/geo.ts` existed (coordinate, ring and transit-fare Zod schemas). There was no `GeocodeProvider`, `RoutingProvider` or `PoiProvider`, and no concrete Nominatim client either — the geocode adapter had not been written at all. Built.                       |
| Seed: cities                                  | implemented  | `seedCities`, all three                                                                                                                                                                                                                                                                |
| Seed: amenities                               | implemented  | `seedAmenities`                                                                                                                                                                                                                                                                        |
| Seed: dev users, Better Auth password hashes  | implemented  | `seedUsers`, D32                                                                                                                                                                                                                                                                       |
| Seed: office locations per user               | implemented  | `seedUsers` writes `OfficeLocation` rows for each dev user                                                                                                                                                                                                                             |
| Seed: listings, images, amenity joins         | implemented  | `seedListings`, D29, D30, D31                                                                                                                                                                                                                                                          |
| Seed: enquiry threads with messages           | implemented  | `seedEngagement`                                                                                                                                                                                                                                                                       |
| Seed: favorites                               | implemented  | `seedEngagement`                                                                                                                                                                                                                                                                       |
| Seed: saved searches                          | implemented  | `seedEngagement`                                                                                                                                                                                                                                                                       |
| **Seed: `ListingView` rows**                  | **missing**  | `grep -n "listingView" packages/db/prisma/seed.ts` returned nothing, so lister analytics and any popularity read were empty on a fresh seed. Added.                                                                                                                                    |
| Seed: current `FuelPrice` rows, all cities    | implemented  | `seedFuelPrices` writes PETROL, DIESEL and CNG per city at an hour-truncated `fetchedAt`, so commute cost works before the scraper's first run.                                                                                                                                        |
| **Seed photos**                               | **reversed** | Eight committed gradient JPEGs (D31). Correction 3 replaces them with real photographs behind a committed manifest.                                                                                                                                                                    |

## 5. Listing CRUD and the image pipeline

| Item                                        | Verdict     | Where                                                                                                                                                                                                                                               |
| ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draft-loose, publish-strict validation      | implemented | D33                                                                                                                                                                                                                                                 |
| Server-owned fields ignored from the client | implemented | D33 and its test                                                                                                                                                                                                                                    |
| Presigned POST with a length condition      | implemented | D36                                                                                                                                                                                                                                                 |
| Magic-byte validation, EXIF stripping       | implemented | D37                                                                                                                                                                                                                                                 |
| Derivation in the worker, not the API       | implemented | D34                                                                                                                                                                                                                                                 |
| Two prefixes, two access policies           | implemented | D35                                                                                                                                                                                                                                                 |
| Rejection distinguished from failure        | implemented | D38                                                                                                                                                                                                                                                 |
| **The Postgres/Redis dual write**           | **missing** | `markImageUploaded` enqueued _before_ updating the row and let an enqueue failure reach the client as a 500 — leaving a committed `PENDING` row that nothing would drain except the 24-hour sweeper, so the upload silently vanished. Correction 4. |

## 6-10. Not yet built at audit time

Map search, the detail sidebar with OSRM and Overpass, the fuel scrapers and
commute-cost engine, the lister dashboard, admin moderation and the final
test/docs pass. These are the continuation, not findings.

The web app as it stood was five auth screens, a stub `HomePage` and a bare
`MapCanvas` — a harness rather than a product, which is what Correction 6 is
about.

---

## Fix log

Where each finding above was closed.

### Correction 1 — Prisma 7 (commit `fix(infra)`)

Moved to Prisma 7.10.0 rather than pinning back: `prisma.config.ts` at the root
carries the schema path, the migrations path and the connection string;
`@prisma/adapter-pg` supplies the connection in `packages/db/src/client.ts`;
`copy-runtime.mjs` is deleted because there is no engine binary to carry any
more. D5 rewritten with the full list, D3 and D7 adjusted so they no longer
contradict it.

Verified: `pnpm db:validate`, `pnpm db:generate`, `pnpm db:migrate` ("Already in
sync"), `pnpm db:reset` (with the consent variable the v7 CLI now demands of an
AI agent), `pnpm db:seed` (51 listings, 9 fuel rows), a live query through the
adapter (`similarity('Koramangala','koramangla')` = 0.643), `pnpm build`,
`pnpm typecheck`, `pnpm lint`, `pnpm test` (107 tests), and
`docker build -f apps/api/Dockerfile` end to end.

Two things the upgrade actually broke, both now fixed rather than silenced:

- the driver adapter's parameter typing made
  `s."price" * (1 - $n)` infer `$n` as `integer`
  (`invalid input syntax for type integer: "0.25"`), failing two
  `findSimilarListings` tests. Fixed with an explicit
  `::double precision` cast, not a schema change.
- both test global-setups ran `prisma migrate deploy` from `packages/db`, where
  v7 cannot find the root config: "The datasource.url property is required in
  your Prisma config file". They now run from the repo root.

`pnpm typecheck` also grew to cover `prisma.config.ts` and `scripts/` through the
new `tsconfig.tools.json` — root-level TypeScript that no workspace package
owned, and therefore the one part of the tree nothing was checking.

### Step 0 — environment findings

`worker` SMTP and mail env added to compose and to its Zod env schema. The
`nominatim-flatnode` volume deleted. `platform: linux/amd64` on all three OSRM
services. `OSRM_CAR_PORT` default moved to 5100. `osrm-init` guards a missing
extract. Both OSRM healthchecks use `driving`. The api env schema now validates
`GEOCODE_PROVIDER`, `NOMINATIM_URL`, both OSRM URLs and the Overpass settings,
which compose was passing in unchecked.

### Correction 2, part 1 — Photon removed

Service, `photon` profile, `photon-data` volume, `PHOTON_URL` and
`GEOCODER_PROVIDER` gone from compose, `.env`, `.env.example` and the README.
`GEOCODE_PROVIDER` added with `nominatim` as its only value. D26 rewritten to
say it was removed, not deferred. There was no Photon branch in a geocode adapter
to delete, because no geocode adapter existed yet — see the next entry.

### Correction 2, part 2 — the provider interfaces

`packages/shared/src/geo/` now exists as the brief declared: `schemas.ts` (the
former `geo.ts`) plus `providers.ts` with `GeocodeProvider`, `RoutingProvider`
and `PoiProvider`, their Zod result schemas, and the autocomplete envelope.

### Correction 4 — the dual write

`markImageUploaded` now commits the row first and enqueues after, and an enqueue
failure is logged rather than returned to the client. A `reconcile-images`
repeatable job runs every 60 seconds, drains `PENDING` rows older than two
minutes that no job is working on, and fails a row after five fruitless
re-enqueues. `reconcileAttempts` added to `ListingImage`. D40 explains why the
row-as-outbox is enough here and names the condition that would justify a real
outbox table.

The worker gained a test setup it did not have (real Postgres, real Redis) and
10 tests, the headline one committing a `PENDING` row with nothing enqueued and
asserting it reaches `READY`.

### Correction 3 — seed photos

`pnpm seed:photos` fetches 23 real house photographs from the Unsplash API into
a committed manifest (`scripts/fixtures/photos.json`) plus a gitignored binary
cache, generates `docs/attribution.md`, and triggers the per-photo download
endpoint the API terms require. The eight gradient JPEGs and
`scripts/generate-fixtures.ts` are deleted. D31 rewritten.

Verified: a cold run fetched all 23 (Unsplash 5xx on some terms is transient and
now retried — "apartment interior" failed four times then succeeded, while
"bedroom" never failed, and curl succeeded throughout, so it is upstream
flakiness rather than the key or the parameters); a warm re-run downloaded 0 and
re-queried nothing; `pnpm db:seed` derived all 23 through the real
`validateAndDerive` and a shared variant fetches anonymously with HTTP 200.

A related divergence the audit had not caught: the first D31 claimed fixtures
were "uploaded once and shared" while the code wrote a private copy per listing.
`ListingImage.variantBaseKey` makes the claim true and removes the duplicated
key derivation from four call sites. D41 covers it, including the deletion guard
that stops one seeded listing's removal from blanking every gallery sharing a
photo.

Still open from Correction 3: the dev-only "seed photos via Unsplash" line on
the listing gallery, which lands with the gallery itself in the UI work.

### Correction 5 — docs out of README

README.md is now the product, the prerequisites, four commands and a table of
links. Everything else moved into `docs/`: `setup.md`, `architecture.md`,
`data-model.md`, `geo.md`, `images.md`, `auth.md`, `design.md`,
`operations.md`, plus the generated `attribution.md` and this audit.
`docs/README.md` is the index, and every document opens with a "read this
when…" line. `DECISIONS.md` stays at the root.

### Correction 6 — design direction

`docs/design.md` commits to a direction before any component is written:
**instrument, not brochure** — the map is the bright real surface, the UI is
calibrated chrome over it, and the palette is derived from the OpenFreeMap
Liberty style's own colours so the two read as one surface. Bricolage Grotesque
for display and price, Instrument Sans for body, IBM Plex Mono for
measurements. A near-square radius scale with round reserved for map-native
objects. Two elevation levels. The signature is a ring gauge that shows which
1/2/3 km band a listing falls in, with a tick for road versus straight-line
distance — the product's argument as a single glyph.

### Step 7 — the detail view

Built: the detail panel (springing sidebar on desktop, drag-dismissable bottom
sheet on mobile, rendered as a child route so the map stays mounted), OSRM car
and bike routes cached 24h with a labelled straight-line fallback, one batched
Overpass query per listing with per-category icon layers whose legend IS the
toggle, nearest-of-each-category with distances, similar listings via the
bounded KNN from D18, and deduplicated `ListingView` increments.

Verified live against the seeded database: 343 Overpass elements parsed into
seven categories with real names and distances (Proteeth Dental 230 m, MedPlus
180 m, Chinmaya Vidyalaya 260 m), the pharmacy layer drawing on the map with
labels at zoom 14+, `police` correctly showing "none within 1.5 km" and
disabled, the similar-listings strip, and the view counter writing exactly one
row and one increment with the second POST deduplicated. 14 new tests.

Three real bugs found and fixed rather than worked around, each with a
DECISIONS entry: Overpass being far too slow for a request path (D42), the
abort-signal composition that silently disabled every geo timeout (D42), and
the detail panel rendering both its desktop and mobile variants so the whole
listing existed twice in the DOM (D45).

**Not verified live:** the route polyline's draw animation and the OSRM road
numbers themselves, because the OSRM graphs need `pnpm bootstrap` (≈1 GB of
downloads plus a long graph build) and were not built in this environment. The
fallback path — a straight-line estimate marked `degraded`, with the warning
sentence in the panel — IS verified, and the polyline code is in place behind
`route.geometry`.

---

## Step 0 — audit of what step 7 built

Run before corrections 7A/7B/7C touched anything, so the "cheap or expensive"
verdict below is about the code as step 7 left it. Every line was checked
against the file named, not recalled.

### Where does the Overpass URL enter the code?

**One env var, behind the adapter. The swap is cheap.**

`grep -rn OVERPASS apps packages scripts docker-compose.yml .env.example`
returns nine hits and no others: the `OVERPASS_URL` / `OVERPASS_TIMEOUT_MS`
declarations in [apps/api/src/env.ts](../apps/api/src/env.ts#L92-L100), the two
uses inside [apps/api/src/geo/overpass.ts](../apps/api/src/geo/overpass.ts)
(the `fetch` target and the `[timeout:]` in the query), the compose
passthrough, and two `.env.example` lines. No feature code, no test and no
second call site mentions a mirror.

The 429/504 handling is inside the adapter too, in one place: `fetchPois`
throws on those two statuses and `warm` catches everything and logs. Nothing
above the adapter knows a rate limit exists — callers see `degraded: true` and
an empty or stale list. So pointing `OVERPASS_URL` at a local service is a
one-line change plus deleting the politeness reasoning.

### The POI and route cache keys, verbatim

As of this audit, and all of them are what correction 7A re-keys:

| What            | Key                                                                   | Where                  |
| --------------- | --------------------------------------------------------------------- | ---------------------- |
| POIs            | `poi:{lat.toFixed(4)},{lng.toFixed(4)}:{Math.round(radiusMeters)}`    | `geo/overpass.ts:170`  |
| POIs, stale     | the same with `:stale` appended                                       | `geo/overpass.ts:189`  |
| Route           | `route:{profile}:{lat4},{lng4}:{lat4},{lng4}`                         | `geo/osrm.ts:63`       |
| Route, stale    | the same with `:stale` appended (written by `cached({keepStaleFor})`) | `lib/cache.ts:83`      |
| Geocode search  | `geocode:nominatim:{normalisedQuery}:{citySlug or 'all'}:{limit}`     | `geo/nominatim.ts:139` |
| Geocode reverse | `geocode:nominatim:reverse:{lat4},{lng4}`                             | `geo/nominatim.ts:183` |

One more Redis key exists — `view:{listingId}:{viewerHash}`
(`services/listing-detail.ts:160`) — and it is deliberately **not** in that
list. A view dedupe window is not derived from OSM data, so prefixing it with a
geo epoch would silently reset every dedupe window on a rebuild.

**A finding while writing them down:** the POI key does not include the
requested categories, while `PoiProvider.nearby` accepts a `categories` subset.
Today the only caller (`getListingPois`) passes none and gets all seven, so
nothing is wrong in practice — but a future caller asking for two categories
would poison the full-set entry. 7A adds the category set to the key.

### Real interfaces, or an inlined Overpass client?

**Real interfaces.** `packages/shared/src/geo/providers.ts` declares
`GeocodeProvider`, `RoutingProvider` and `PoiProvider` with Zod result schemas
and imports nothing about any concrete service; the clients live in
`apps/api/src/geo/` behind `resolveGeocodeProvider()`, `resolveRoutingProvider()`
and `resolvePoiProvider()`. Nothing to fix here — the interfaces were built in
correction 2, part 2.

### Does anything assume planet-current OSM?

**Yes, and only the POIs.** Routing and geocoding already read the fixed extract
(`osm-data/merged.osm.pbf`, cut on 2026-09-02); the POI panel was the one
surface reading live planet data through a public mirror. Two concrete
consequences:

- Every POI number in the step 7 section above — 343 elements, Proteeth Dental
  at 230 m, MedPlus at 180 m, Chinmaya Vidyalaya at 260 m — was measured
  against planet-current data and **is not evidence about the extract**. It is
  re-verified after the rebuild, later in this document.
- A listing's road graph and its nearby-hospital list could disagree about what
  exists. That is unfalsifiable in a bug report, and it is the substantive
  reason for correction 7C rather than the fair-use one.

Nothing else reads live OSM: `grep -rn "openstreetmap.org|overpass" apps/web/src`
returns only the attribution string in the map style credit.

### The items with no decisions entry closing them

| Question                    | Answer                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `api` healthcheck           | **partial — fixed.** It exists, but only as `HEALTHCHECK` in `apps/api/Dockerfile:41`. `web` declares `depends_on: api: service_healthy` in compose, so the dependency and the check lived in different files; a compose run against a prebuilt or retagged image without that layer would wait forever on a dependency that can never report healthy. Moved into compose, where the dependency is declared. |
| `worker` healthcheck        | **partial — fixed.** Same shape: `apps/worker/Dockerfile:37`. Moved for the same reason, and because step 8's fuel scraper makes the worker something other services start depending on.                                                                                                                                                                                                                     |
| `worker` S3 env             | implemented. Endpoint, region, bucket, both keys, `S3_FORCE_PATH_STYLE`, `KEEP_ORIGINALS`, `IMAGE_CONCURRENCY` — all present in the `worker` service block.                                                                                                                                                                                                                                                  |
| `worker` SMTP env           | implemented. `SMTP_HOST/PORT/SECURE/USER/PASSWORD`, `MAIL_FROM` and `WEB_APP_URL`, added by the correction-1 sweep and still there.                                                                                                                                                                                                                                                                          |
| Google + GitHub, env-gated  | implemented. `socialProviders()` in `apps/api/src/auth/index.ts:19` wires a provider only when it is in `AUTH_ENABLED_PROVIDERS` **and** has a non-empty id and secret.                                                                                                                                                                                                                                      |
| admin plugin mounted        | implemented. `adminPlugin({ defaultRole: DEFAULT_ROLE, adminRoles: ['ADMIN'] })`, line 130.                                                                                                                                                                                                                                                                                                                  |
| openAPI dev-only            | implemented. `...(isProduction ? [] : [openAPI()])`, line 132.                                                                                                                                                                                                                                                                                                                                               |
| `requireRole` test coverage | implemented. `apps/api/test/auth-guards.test.ts` covers unauthenticated (401), wrong role (403), wrong owner (`forbidden_owner`) and both admin overrides.                                                                                                                                                                                                                                                   |
| seed completeness           | implemented. `seedAmenities`, `seedCities`, `seedUsers` (offices per user), `seedListings`, `seedEngagement` (enquiry threads with messages, favorites, saved searches), `seedListingViews` and `seedFuelPrices` all exist. The last is what step 8 needs on a fresh seed.                                                                                                                                   |
| cache TTLs                  | **no drift.** `packages/shared/src/constants.ts`: `poi` 86400, `route` 86400, `geocode` 604800, `fuelPrice` 3600 — exactly 24h / 24h / 7d / 1h.                                                                                                                                                                                                                                                              |

### One place the code contradicts a decisions entry

D42 states that `qt` "is now in the query", having measured that quadtile
ordering is the difference between an answer and a timeout. The query builder
emits `out center tags;` — **without `qt`**
([geo/overpass.ts:143](../apps/api/src/geo/overpass.ts#L143)). The measurement
stands and the code did not follow it. Fixed under correction 7C rather than by
weakening the entry: a local instance is faster but not immune, and an ordering
that costs nothing to ask for should be asked for.

---

## The rebuild, and step 7 re-verified against it

Everything below was run, not reasoned about. The artifacts were re-cut with
9 km of padding (D47), re-merged, both OSRM graphs rebuilt, Nominatim
re-imported and Overpass initialised for the first time — then step 7's claims
were checked again, because the POI numbers in the step 7 section above were
measured against planet-current data through a public mirror and are **not**
evidence about the extract.

### What the rebuild produced

| Artifact            | Before         | After          |
| ------------------- | -------------- | -------------- |
| `patna.osm.pbf`     | 2.4 MB         | 4.0 MB         |
| `bengaluru.osm.pbf` | 38.4 MB        | 43.3 MB        |
| `pune.osm.pbf`      | 14.8 MB        | 18.5 MB        |
| `merged.osm.pbf`    | 53.2 MB        | 63.1 MB        |
| geo config hash     | `2e8d102364f5` | `c06555944c3a` |
| geo epoch           | `cfd8a4dcf033` | `f646d2f4732d` |

The epoch moving on its own is D46 working as designed: adding `paddedBbox` to
the hashed config changed the hash, so every route and POI cached against the
old cuts became unreachable with nothing flushed. Directly visible in
`redis-cli --scan`: the pre-correction `route:bike:25.6191,85.1767:…` keys are
still present and now unreachable, next to `f646d2f4732d:route:car:…`.

The bounds-stamp change (D47) was what made the re-cut happen at all. The run
log reads `patna bounds changed (unstamped -> 84.8603,25.4186,85.3897,25.7614)`
for each city — under the old "the file exists, skip it" rule every cut would
have been kept and the padding would have looked applied while changing
nothing.

### Padding, verified live

`apps/api/test/geo-boundary.test.ts`, against the rebuilt artifacts:

- **A route across a former cut edge.** Whitefield (77.77) to Hoskote (77.83)
  straddles Bengaluru's old `maxLng` of 77.78. It now returns real geometry,
  not `degraded`: **9.82 km / 11.3 min by car**, 10.33 km / 42.1 min by bike,
  against 6.5 km straight-line. Inside the assertion band (longer than
  straight-line, under 2.5×) — so no absurd detour, which is the other
  unpadded-cut symptom.
- **POIs on both sides of a former edge.** The first version of this test
  probed the eastern edge and found **two** places within 1.5 km, because that
  stretch towards Hoskote is genuinely empty in OSM. A test that cannot tell
  "we cut the map here" from "nothing is here" proves nothing, so the probe
  moved to the northern edge at Jakkur (13.14, 77.57): **39 places within
  1.5 km, 9 of them north of the old cut** — data that did not exist in the old
  extract at all.

### Overpass, self-hosted (D48)

Initialised from the same `merged.osm.pbf`. Serving, with
`timestamp_osm_base: 2026-09-01T18:30:45Z` — the extract's own snapshot, which
is the point: routing, geocoding and POIs now cannot disagree about what
exists.

**One real bug found, and it is the kind that would have been very hard to
diagnose later.** With the image's `/db` mounted as a **named volume**, every
query failed with:

```
runtime error: open64: 13 Permission denied /db/db//osm3s_osm_base
```

while the container reported healthy, the import log said it finished, and the
2 GB database sat there intact. Cause: `/db` inside the image is the `overpass`
user's home directory, mode **0700**, and a named volume inherits that mode when
Docker seeds it — but `fcgiwrap`, which executes every query, runs as **nginx**,
not as `overpass`. So the query process could not traverse into `/db`. A bind
mount never shows this, because host directories are 0755. Fixed in
`docker-compose.yml` with `chmod 0755 /db` before the image's own entrypoint.

Verified after the fix: a seven-category query at Koramangala returns real
named results from the local instance.

### Nominatim, re-imported

The volume could not be dropped from this session (the sandbox refused
`docker volume rm`), so the re-import was done through the service's own
tooling instead — `DROP DATABASE nominatim` followed by
`nominatim import --osm-file /osm/merged.osm.pbf`, which is what the image's
`init.sh` does on first boot. **8 minutes.** Verified afterwards: `/status`
answers 200 and a `Koramangala` search returns the suburb at 12.9357, 77.6241.

### The city boundaries (D50)

Derived from the re-imported Nominatim, simplified and committed:

| City      | Source                                | Points     | Area stored |
| --------- | ------------------------------------- | ---------- | ----------- |
| Patna     | `way/383774533`, settlement search    | 2000 → 628 | 136 km²     |
| Bengaluru | `relation/7902476`, settlement search | 4981 → 335 | 716 km²     |
| Pune      | `relation/10351626`, reverse lookup   | 345 → 193  | 312 km²     |

Patna Municipal Corporation is about 136 km² and BBMP about 709 km², so the
first two are the genuine municipal boundaries. Pune is the exception the
brief predicted for Bengaluru: the city is mapped only as a **node** in this
extract, so the fallback resolved the enclosing subdistrict relation — larger
than the municipal boundary, and accepted deliberately, because a generous
boundary still answers "which city is this listing in" correctly where the
alternative is a guess.

Against the seeded data, `ST_Covers` puts **40/40 Bengaluru, 37/40 Patna and
35/40 Pune** listings inside their own city's boundary. The remainder sit just
outside a tight municipal polygon and resolve through the nearest-centroid
fallback — correctly, at these distances, and **labelled** as guesses, which is
the whole design (D50).

### Step 7's other claims, re-checked

| Claim from step 7                         | Verdict after the rebuild                                                                                                                          |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Seven POI categories from a batched query | Holds, now against the local instance. The old element counts are superseded — they were planet-current mirror data.                               |
| Nearest-of-each distances are sane        | Holds; the POI probe returns named places with distances in the tens to hundreds of metres.                                                        |
| OSRM routes render for a listing          | Now verified **live** for the first time — the step 7 section explicitly could not, because the graphs were not built. 9.82 km car, 10.33 km bike. |
| The bounded KNN for similar listings      | Holds; `packages/db` tests pass against the rebuilt database (47 tests).                                                                           |
| The straight-line fallback is labelled    | Holds, and still covered by tests with OSRM stubbed out.                                                                                           |

### Full check after the rebuild

`pnpm typecheck` exits 0 across every package including the tools tsconfig;
`pnpm lint` and `pnpm format:check` are clean; `pnpm test` is **212 tests
passing** (shared 31, db 47, api 124, worker 10), with the two live geo tests
skipping themselves when the geo profile is down. `pnpm cities:validate` passes
with no warnings and `pnpm geo:status` reports every artifact `ok`.

---

# Final reconciliation — the brief, item by item

**Read this when** you want the one-line verdict on every item the original
brief asked for, after steps 9 and 10.

The sections above audit steps 1-5 as they stood at commit `6c5050a` and then
record each correction. This section closes the loop: every brief item, its
verdict now, and a link to the entry explaining any divergence. **Where
something was dropped, it says so.** An audit that lists only successes is not
one.

Verdicts: **implemented** / **diverged** (with an entry) / **partial** /
**dropped**.

## Step 1 — monorepo, compose, CI

| Item                                | Verdict      | Note                                                                                            |
| ----------------------------------- | ------------ | ----------------------------------------------------------------------------------------------- |
| pnpm workspace, five packages       | **diverged** | Six. `apps/e2e` was added in step 10 for the full-path suite; sharp must not enter `apps/api`.  |
| Docker compose for every dependency | implemented  | Core stack plus a `geo` profile that needs artifacts first (D6).                                |
| Prisma 6                            | **diverged** | Prisma 7. A v7 CLI cannot read a v6 schema, so there was no half-way state (D5).                |
| CI: typecheck, lint, test, build    | implemented  | Plus `cities:validate`, which fails CI on a malformed city record (D49).                        |
| Health endpoints                    | implemented  | `/health` changes status code as well as body; `/health/geo` is separate on purpose (D10, D51). |
| Alpine images                       | **diverged** | `node:22-bookworm-slim` for API and worker; native prebuilds need glibc (D7).                   |

## Step 2 — data model

| Item                                     | Verdict      | Note                                                                                                       |
| ---------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------- |
| PostGIS geography columns                | implemented  | Optional in the schema, NOT NULL by CHECK — a required `Unsupported()` field breaks `create` (D15).        |
| lat/lng kept in sync                     | implemented  | A `BEFORE INSERT OR UPDATE` trigger, never application code (D2).                                          |
| "Prisma cannot generate spatial indexes" | **diverged** | It can, and introspects them — hand-written ones were being DROPped every `migrate dev` (D13).             |
| Shared Zod schemas derived from Prisma   | **diverged** | Derivation would be a cycle; the values live in `shared` with a type-level guard proving they match (D14). |
| Keyset pagination                        | implemented  | Row-constructor on `(sortKey, id)`; `OFFSET` skips rows under concurrent publishes (D19).                  |
| Money as Int rupees                      | implemented  | `FuelPrice.price` is the deliberate `Decimal(8,2)` exception (D17).                                        |

## Step 3 — auth

| Item                               | Verdict                 | Note                                                                                                                                                 |
| ---------------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Better Auth, self-hosted           | implemented             | Sessions in Postgres with Redis in front; `storeSessionInDatabase` is load-bearing (D20).                                                            |
| Email + Google + GitHub            | implemented             | A provider is wired only when enabled **and** configured — half-configured OAuth is worse than absent.                                               |
| Role guards                        | implemented             | Rank-based, admin-override, and every mutation re-checks ownership server-side.                                                                      |
| Admin plugin                       | **corrected in step 9** | Mounted since step 3 and **inert**: `adminRoles` is not the permission check, and all of `list-users`, `ban-user` and `set-role` answered 403 (D70). |
| Rate limiting on credential routes | implemented             | Counters in Redis; route names verified live because 1.7 renamed one (D23).                                                                          |

## Step 4 — bootstrap, geo services, seed

| Item                                  | Verdict      | Note                                                                                                                              |
| ------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Photon for autocomplete               | **dropped**  | Cannot read a `.osm.pbf`, has no published image, and its per-country index is gone. Replaced by a local pg_trgm tier (D26, D39). |
| Nominatim, OSRM, Overpass             | implemented  | All three self-hosted from one merged extract; a public mirror is a fallback (D48).                                               |
| "Geofabrik publishes India per-state" | **diverged** | It publishes six zones; the per-state URLs return HTML, which fails much later as a corrupt download (D27).                       |
| Deterministic seed                    | implemented  | Fixed-seed PRNG, and it prunes listings it no longer plans (D29).                                                                 |
| Seed photographs                      | **diverged** | Real Unsplash photographs from a committed manifest, not the gradients first chosen (D31).                                        |

## Step 5 — listing CRUD and images

| Item                               | Verdict      | Note                                                                                               |
| ---------------------------------- | ------------ | -------------------------------------------------------------------------------------------------- |
| Listing CRUD with ownership checks | implemented  | Owner id always from the stored row (D33).                                                         |
| sharp in the API                   | **diverged** | Derivation is worker-only behind `@machiya/shared/images`; the API reads no image bytes (D34).     |
| Presigned upload                   | **diverged** | POST, not PUT: only a POST policy can bound the body length (D36).                                 |
| Content-type validation            | implemented  | Magic bytes decide; EXIF stripped, animated inputs refused (D37).                                  |
| Public bucket                      | **diverged** | `variants/` only. The original grant was bucket-wide, which served every unvalidated upload (D35). |

## Steps 6-8 — search, detail, commute

| Item                                 | Verdict      | Note                                                                                                  |
| ------------------------------------ | ------------ | ----------------------------------------------------------------------------------------------------- |
| Radius search with rings and filters | implemented  | One parameterised statement; nothing filters a superset in JavaScript.                                |
| Shareable URL state                  | implemented  | The URL is the search state, and a round-trip test proves it.                                         |
| Detail view as a sibling route       | **diverged** | A child route, so the WebGL map is not torn down on every navigation (D45).                           |
| POIs from a public Overpass          | **diverged** | Self-hosted, after measuring 38-85 s and a 406-to-everything mirror (D42, D48).                       |
| Commute cost from real road distance | implemented  | One `/table` call per search, not one `/route` per listing (D61).                                     |
| Three fuel sources                   | **diverged** | Three names, **two** feeds. Two of the brief's sources served another city's price at HTTP 200 (D62). |
| Sort by total monthly cost           | implemented  | Inside the SQL, across the whole radius and correct across pages (D64).                               |

## Step 9 — lister dashboard, enquiries, admin

| Item                                                          | Verdict      | Note                                                                                                                                                                                                                 |
| ------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gated by `requireRole('lister','admin')`                      | **diverged** | The wizard is **not** role-gated: a seeker drafts and is upgraded by publishing, so gating the entrance on the role you earn by walking through it is backwards. The upgrade happens inside the publish transaction. |
| Multi-step resumable wizard, server autosave                  | implemented  | Seven steps, step in the URL, draft id in the path; nothing that matters is in the browser (D67).                                                                                                                    |
| D59's three address rules                                     | implemented  | Locality prefilled only on a locality-level match, street line never prefilled, and no draft without a pin.                                                                                                          |
| Coverage checked on the coordinate                            | implemented  | Refusal blocks the step and renders served cities as buttons from the 422's payload (D53).                                                                                                                           |
| City resolved from coordinates                                | implemented  | No city control on the screen; re-resolved whenever the pin moves (D50).                                                                                                                                             |
| Photos: presigned, drag-reorder, cover, compression, progress | implemented  | Reorder and cover never re-run derivation (D41); XHR for progress because fetch cannot report it.                                                                                                                    |
| PENDING / REJECTED / FAILED distinguished                     | implemented  | PENDING is the calm state — it is the good case and the most common (D38).                                                                                                                                           |
| Publish requires a READY image, 422 says which                | implemented  | Both messages asserted in `apps/e2e`.                                                                                                                                                                                |
| My-listings table with counts                                 | implemented  | Edit, duplicate, pause, mark-rented. Duplicate does **not** copy photos (D41).                                                                                                                                       |
| Analytics: views over time, conversion                        | implemented  | The caption states D44's definition using the window the API sends, so the two cannot drift.                                                                                                                         |
| Threaded enquiries, unread counts                             | implemented  | One thread per seeker per listing, both sides on one screen.                                                                                                                                                         |
| Contact masked until an enquiry is sent                       | implemented  | Decided server-side, and re-masked when a lister closes the thread (D69).                                                                                                                                            |
| Email on the first message only                               | **diverged** | "First message **in a while**". The literal rule sends nothing when a quiet thread resumes, which is exactly when someone needs telling (D68).                                                                       |
| Mail from the worker                                          | implemented  | Its own transport, which throws where the API's swallows (D68).                                                                                                                                                      |
| Reconcile pattern for notifications                           | implemented  | Decided yes, and why: a dropped notification is invisible to everyone, unlike a stalled image (D68). Verified live at 110 s.                                                                                         |
| Favorites with optimistic toggles                             | implemented  | Rollback restores the previous set rather than re-inverting.                                                                                                                                                         |
| Saved searches with office coordinates                        | implemented  | Coverage checked on read; an uncovered office says so and offers the nearest served city.                                                                                                                            |
| Moderation queue, verify/unverify                             | implemented  | Oldest-first, and unverify lives on the verified list because the queue holds only unverified rows.                                                                                                                  |
| User management via the admin plugin                          | implemented  | After D70 made the plugin work at all.                                                                                                                                                                               |
| Fuel scrape health surfaced                                   | implemented  | It existed at a URL nothing linked to; now a tab (D71).                                                                                                                                                              |
| CoverageRequest clusters surfaced                             | implemented  | Spatially clustered, ranked by distinct people (D71).                                                                                                                                                                |
| Designed empty states on every new screen                     | implemented  | Listings, enquiries, favourites, saved searches, moderation, coverage demand.                                                                                                                                        |

## Step 10 — close-out

| Item                                         | Verdict     | Note                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Full test pass                               | implemented | 393 tests across six packages, all passing.                                                                                                                                                                                                                                                                        |
| Live-service coverage where a stub hid a bug | implemented | Closed in correction 12. `apps/e2e` walks sign-up to enquiry with nothing stubbed; `nominatim-live.test.ts` re-runs D57's, D58's and D60's own measurements against the real geocoder — and found that the first version of itself passed from cache, including against an upstream that 403s every request (D74). |
| Docs complete, each linking to its entry     | implemented | Plus `why.md`, which the brief did not ask for.                                                                                                                                                                                                                                                                    |
| `known-issues.md` pruned                     | implemented | Four entries closed by steps 9 and 10; the rest are genuinely open.                                                                                                                                                                                                                                                |
| README rewritten as a front door             | implemented | Prerequisites, four commands, dev accounts, and a table into `docs/`.                                                                                                                                                                                                                                              |
| This audit section                           | implemented | You are reading it.                                                                                                                                                                                                                                                                                                |

## What was dropped, plainly

Four things the brief asked for are not in the product, and none of them is an
oversight:

1. **Photon.** It cannot index an `.osm.pbf` without a Nominatim to read from,
   komoot publishes no image, and the per-country prebuilt index is gone —
   leaving a 101 GB planet download. Autocomplete is a local pg_trgm tier with
   Nominatim behind it, which is faster than Photon would have been (D26, D39).
2. **Two of the three named fuel sources.** `mypetrolprice` serves the Delhi
   shell for every city, and one more returned a national figure on a city page.
   Both answered HTTP 200 with a plausible wrong price (D62).
3. **The role gate on the wizard.** Deliberately inverted, as above.
4. **`impersonate-user`.** The admin plugin ships it; no role is granted it. An
   operator reading somebody's private enquiries as them is a capability this
   product has no use for (D70).

One thing is **present but weaker than it reads**:

- **The seeded engagement data is one lister's.** Every seeded listing is owned
  by `lister@dev.local` (D29), so the admin user list and the moderation queue
  look less varied than a real deployment would.

Transit fares were the second entry here — configuration rather than scraped,
going stale in silence. They are still configuration, because no free API
publishes them, but the silence is gone: `cities:validate` warns past a year and
fails CI on a malformed review date (D73).

## Gates, re-run for this section

Every command below was run at the commit that added this section, and the
counts were refreshed in correction 12:

| Command                      | Result                                                                      |
| ---------------------------- | --------------------------------------------------------------------------- |
| `pnpm typecheck`             | exits 0 across six packages, tests included                                 |
| `pnpm lint`                  | clean, no warnings                                                          |
| `pnpm format:check`          | "All matched files use Prettier code style!"                                |
| `pnpm test`                  | **404 passing** — shared 88, web 14, db 59, api 217, worker 24, e2e 2       |
| `pnpm cities:validate`       | "Validated 3 cities… OK — no issues."                                       |
| `pnpm geo:status`            | every artifact `ok`, epoch `f646d2f4732d`                                   |
| `pnpm -F @machiya/web build` | 1,806 KB main chunk, down from 2,264 KB before the lister routes were split |

The web app contributed **zero** tests until this step and its sources were
never compiled by the gate. Turning that on immediately surfaced a real config
fault — a test tsconfig's `types` list replaces rather than extends, so dropping
`vite/client` stopped four source files compiling. That is the third time a gate
being switched on has found something (D65 was the first, this is the third),
which is the argument for switching them on.
